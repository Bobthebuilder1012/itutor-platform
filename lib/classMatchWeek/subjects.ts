/**
 * Moved to `lib/matching/subjects.ts`, which is shared with Find Your iTutor.
 *
 * See the note in `./levels.ts`. Prefer `@/lib/matching/subjects` in new code.
 */
export * from '@/lib/matching/subjects';
